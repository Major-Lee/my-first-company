package helper;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.smartwork.msip.cores.helper.HttpHelper;

public class GomeCallbackHelper {
	private final Logger logger = LoggerFactory.getLogger(GomeCallbackHelper.class);

	public static void notify(String mac, int online){
//		logger.info(String.format("Request  mac[%s] online[%s]", mac, online));
//		String response = HttpHelper.postUrlAsString(urlApi, api_params,Common_Headers);
//		logger.info(String.format("Response url[%s] params[%s] response[%s]", urlApi, api_params, response));
//		return response;

	}
}
