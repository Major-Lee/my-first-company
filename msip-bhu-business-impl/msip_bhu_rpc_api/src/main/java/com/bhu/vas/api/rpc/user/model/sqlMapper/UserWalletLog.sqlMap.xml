<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bhu.vas.api.rpc.user.model.UserWalletLog">

  <resultMap id="modelResult" type="userWalletLog">
    <id property="id" column="id"/>
    <result property="uid" column="uid"/>
    <result property="orderid" column="orderid"/>
    <result property="transmode" column="transmode"/>
    <result property="transtype" column="transtype"/>
    <result property="transmode_desc" column="transmode_desc"/>
    <result property="transtype_desc" column="transtype_desc"/>    
    <result property="rmoney" column="rmoney"/>
    <result property="cash" column="cash"/>
    <result property="vcurrency" column="vcurrency"/>
    <result property="mac" column="mac"/>
    <result property="current_gpath" column="current_gpath"/>
    <result property="memo" column="memo"/>
    <result property="description" column="description"/>
    <result property="role" column="role"/>
    
    <result property="updated_at" column="updated_at"/>
  </resultMap>  
  <resultMap id="mapcountAndsum" type="hashmap">
    <result property="cashSum" column="cashSum" javaType="java.lang.Double"/>
    <result property="count" column="count" javaType="java.lang.Long"/>
  </resultMap>
  
   <resultMap id="mapqueryRewardpages" type="hashmap">
   		<result property="orderid" column="orderid" javaType="java.lang.String"/>
   		<result property="cash" column="cash" javaType="java.lang.String"/>
   		<result property="amount" column="amount" javaType="java.lang.String"/>
   		<result property="mac" column="mac" javaType="java.lang.String"/>
   		<result property="umac" column="umac" javaType="java.lang.String"/>
   		<result property="payment_type" column="payment_type" javaType="java.lang.String"/>
   		<result property="umactype" column="umactype" javaType="java.lang.Integer"/>
   		<result property="paymented_at" column="paymented_at" javaType="java.lang.String"/>
   		<result property="created_at" column="created_at" javaType="java.lang.String"/>
   </resultMap>  
  
  	<sql id="modelTableName">
	    <![CDATA[
	    	t_vas_users_wallet_log
	    ]]>
	</sql>
	<sql id="modelColumns">
	    <![CDATA[
	    	id,
	    	uid,
	    	orderid,
	    	transmode,
	    	transtype,
	    	transmode_desc,
	    	transtype_desc,
	    	rmoney,
	    	cash,
	    	vcurrency,
	    	mac,
	    	current_gpath,
	    	memo,
	    	description,
	    	role,
	    	updated_at
	    ]]>
	</sql>  
	<sql id="modelWithoutPKColumns">
	    <![CDATA[
	    	uid,
	    	orderid,
	    	transmode,
	    	transtype,
	    	transmode_desc,
	    	transtype_desc,
	    	rmoney,
	    	cash,
	    	vcurrency,
	    	mac,
	    	current_gpath,
	    	memo,
	    	description,
	    	role,
	    	updated_at
	    ]]>
	</sql> 	
	
	<sql id="modelInsertValues">
			#{id},
	    	#{uid},
	    	#{orderid},
	    	#{transmode},
	    	#{transtype},
	    	#{transmode_desc},
	    	#{transtype_desc},	    	
	    	#{rmoney},
	    	#{cash},
	    	#{vcurrency},
	    	#{mac},
	    	#{current_gpath},
	    	#{memo},
	    	#{description},
	    	#{role},
	    	#{updated_at}
	</sql>
		
	<sql id="modelUpdateValues">
	    	uid = #{uid},
	    	orderid = #{orderid},
	    	transmode = #{transmode},
	    	transtype = #{transtype},
	    	transmode_desc = #{transmode_desc},
	    	transtype_desc = #{transtype_desc},
	    	rmoney = #{rmoney},
	    	cash = #{cash},
	    	vcurrency = #{vcurrency},
	    	mac = #{mac},
	    	current_gpath = #{current_gpath},
	    	memo = #{memo},
	    	description = #{description},
	    	role = #{role},
	    	updated_at = #{updated_at}
	</sql>
	<sql id="model_CRITERIA_SQL_GenCondition">
		<!--
			WARNING - @mbggenerated This element is automatically generated by
			MyBatis Generator, do not modify. This element was generated on Thu
			Sep 01 10:08:21 CST 2011.
		-->
		<where>
			<foreach collection="oredCriteria" item="criteria" separator="or">
				<if test="criteria.valid">
					<trim prefix="(" suffix=")" prefixOverrides="and">
						<foreach collection="criteria.criteria" item="criterion">
							<choose>
								<when test="criterion.noValue">
									and ${criterion.condition}
                				</when>
								<when test="criterion.singleValue">
									and ${criterion.condition} #{criterion.value}
                				</when>
								<when test="criterion.betweenValue">
									and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                				</when>
								<when test="criterion.listValue">
									and ${criterion.condition}
									<foreach collection="criterion.value" item="listItem" open="(" close=")" separator=",">
										#{listItem}
                  					</foreach>
								</when>
							</choose>
						</foreach>
					</trim>
				</if>
			</foreach>
		</where>
	</sql>
	
	<select id="select" resultMap="modelResult">
	    select <include refid="modelColumns" /> from <include refid="modelTableName" /> 
	    <where>
	    	<if test="_parameter != null">
				id = #{id}
				<!-- 
					#{value}
					#{_parameter}
				 -->
			</if>
		</where>
	</select>
	
	<select id="countAndsum" resultMap="mapcountAndsum" parameterType="Map">
	    select truncate(sum(t_walletlog.cash),2) as cashSum,count(*) as count from <include refid="modelTableName" /> t_walletlog,msip_bhu_commdity.t_commdity_orders t_orders
	    <where>
	    	t_walletlog.uid = #{uid}
	    	AND t_walletlog.transtype = 'P2C'
	    	<if test="start_time != null">
	    		AND t_orders.created_at &gt;= #{start_time}
	    	</if>
	    	<if test="end_time != null">
	    		AND t_orders.created_at &lt;= #{end_time}
	    	</if>
	    	AND t_walletlog.orderid = t_orders.id
	    	<if test="mac != null">
	    		AND	t_walletlog.mac = #{mac}
	    	</if>
	    	<if test="umac != null">
	    		AND	t_orders.umac = #{umac}
	    	</if>
	    	<if test="dut != null">
	    		AND	t_orders.mac_dut = #{dut}
	    	</if>
	    	<if test="type != null">
	    		AND t_orders.type = #{type}
	    	</if>
	    	<if test="status != null">
	    		AND t_orders.status = #{status}
	    	</if>
	    	<if test="role != null">
	    		AND t_walletlog.role = #{role}
	    	</if>
	    </where> 
	</select>
	
	<select id="queryRewardpages" resultMap="mapqueryRewardpages" parameterType="Map">
	    select 
	    t_walletlog.orderid as orderid,
		truncate(t_walletlog.cash,2) as cash,
		t_orders.amount as amount,
		t_orders.mac as mac,
		t_orders.umac as umac,
		t_orders.payment_type as payment_type,
		t_orders.umactype as umactype,
		t_orders.paymented_at as paymented_at,
		t_orders.created_at as created_at 
	    from <include refid="modelTableName" /> t_walletlog,msip_bhu_commdity.t_commdity_orders t_orders
	    <where>
	    	t_walletlog.uid = #{uid}
	    	AND t_walletlog.transtype = 'P2C'
	    	<if test="start_created_ts != null">
	    		AND t_orders.created_at &gt;= #{start_created_ts}
	    	</if>
	    	<if test="end_created_ts != null">
	    		AND t_orders.created_at &lt;= #{end_created_ts}
	    	</if>
	    	AND t_walletlog.orderid = t_orders.id
	    	<if test="mac != null">
	    		AND	t_orders.mac = #{mac}
	    	</if>
	    	<if test="umac != null">
	    		AND	t_orders.umac = #{umac}
	    	</if>
	    	<if test="dut != null">
	    		AND	t_orders.mac_dut = #{dut}
	    	</if>
	    	<if test="type != null">
	    		AND t_orders.type = #{type}
	    	</if>
	    	<if test="status != null">
	    		AND t_orders.status = #{status}
	    	</if>
	    	<if test="role != null">
	    		AND t_walletlog.role = #{role}
	    	</if>
	    	ORDER BY t_orders.updated_at DESC
	    	limit #{limit} OFFSET #{start}
	    </where> 
	</select>
	
	<delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
		delete from <include refid="modelTableName" /> where id=#{id}
	</delete>
	
	<delete id="delete" parameterType="userWalletLog">
		delete from <include refid="modelTableName" />
		<where>
			<if test="id != null">
				id = #{id}
			</if>
		</where>
	</delete>
	<insert id="insert" parameterType="userWalletLog">
		insert into <include refid="modelTableName" /> (<include refid="modelColumns" />) values (<include refid="modelInsertValues" />);
		<selectKey keyProperty="id" resultType="java.lang.Long" order="AFTER">
			select last_insert_id(); 
		</selectKey>
	</insert>
 
	<update id="update" parameterType="userWalletLog">
	    update <include refid="modelTableName" /> set <include refid="modelUpdateValues" /> WHERE id = #{id}
	</update>
		
	<select id="selectByIds" resultMap="modelResult" parameterType="java.util.List">
	  	select <include refid="modelColumns" /> from <include refid="modelTableName" />
	  	<where>
	  		id IN
	  		<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
			    #{item}
			</foreach>
	  	</where>
	</select>
	  
	<delete id="deleteByIds" parameterType="java.util.List">
	  	delete from <include refid="modelTableName" />
	  	<where>
	  		id IN
	  		<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
			    #{item}
			</foreach>
	  	</where>
	</delete>
	
	<select id="pkSelectByModelCriteria" resultType="long">
		select id from <include refid="modelTableName" /> 
		<if test="_parameter != null"> 
	     	<include refid="model_CRITERIA_SQL_GenCondition" />
	      	<if test="orderByClause != null">
				order by ${orderByClause}
	    	</if>
			<if test="start!=0 or limit!=0">
				limit #{limit} OFFSET #{start}
			</if>
	    </if>  
	</select>
	
	<select id="countByModelCriteria" resultType="int">
		select count(*) from <include refid="modelTableName" /> 
		<if test="_parameter != null"> 
	      <include refid="model_CRITERIA_SQL_GenCondition" />
	    </if>  		
	</select>
	<select id="countIncome" resultType="Map" parameterType="Map">
		SELECT sum(cash) as cash,count(*) as num from t_vas_users_wallet_log  WHERE   transmode='SDP' and transtype='P2C' 	
		<if test="uid!=null">
			and uid=#{uid} 
		</if>
		<if test="mac!=null">
			and mac=#{mac}
		</if>	
		<if test="role!=null">
			and role=#{role}
		</if>
		<if test="start_created_ts!=null">
			and updated_at >= #{start_created_ts}
		</if>
		<if test="end_created_ts!=null">
			and <![CDATA[ updated_at <= #{end_created_ts} ]]> 
		</if>
	</select>
	<select id="countDealIncome" resultType="Map" parameterType="Map">
		SELECT SUM(a.amount) as amount FROM( 	SELECT amount from t_vas_users_wallet_log l,msip_bhu_commdity.t_commdity_orders o WHERE  l.orderid=o.id and l.transmode='SDP' and l.transtype='P2C' 	
		<if test="uid!=null">
			and l.uid=#{uid} 
		</if>
		<if test="mac!=null">
			and l.mac=#{mac}
		</if>	
		<if test="role!=null">
			and l.role=#{role}
		</if>
		<if test="start_created_ts!=null">
			and l.updated_at >= #{start_created_ts}
		</if>
		<if test="end_created_ts!=null">
			and <![CDATA[ l.updated_at <= #{end_created_ts} ]]> 
		</if>
		group by l.orderid ) as a
	</select>
	
	<select id="accountIncome" resultType="Map" parameterType="Map">
		SELECT SUM(o.amount) as amount,sum(l.cash) as cash,count(*) as num from t_vas_users_wallet_log l,msip_bhu_commdity.t_commdity_orders o
		 WHERE l.uid=#{uid} AND l.orderid=o.id and (l.transmode='SDP' and l.transmode='DWP') and (l.transtype='P2C' and l.transtype='R2C') 	
		<if test="mac!=null">
			and l.mac=#{mac}
		</if>	
		<if test="role!=null">
			and l.role=#{role}
		</if>
		<if test="start_created_ts!=null">
			and l.updated_at >= #{start_created_ts}
		</if>
		<if test="end_created_ts!=null">
			and <![CDATA[ l.updated_at <= #{end_created_ts} ]]> 
		</if>
	</select>
	
	<delete id="removeByModelCriteria">
		delete from <include refid="modelTableName" />  
		<if test="_parameter != null"> 
	      <include refid="model_CRITERIA_SQL_GenCondition" />
    	</if>  
	</delete>
	<select id="executeProcdure" parameterType="java.lang.Object" statementType="CALLABLE" resultType="int">
		${proc_call} 		
	</select>
</mapper>