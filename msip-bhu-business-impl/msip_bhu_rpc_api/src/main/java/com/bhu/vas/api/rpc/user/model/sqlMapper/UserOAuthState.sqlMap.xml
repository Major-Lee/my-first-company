<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bhu.vas.api.rpc.user.model.UserOAuthState">

  <resultMap id="modelResult" type="userOAuthState">
    <result property="uid" column="uid"/>
    <result property="auid" column="auid"/>
    <result property="identify" column="identify"/>
    <result property="updated_at" column="updated_at"/>
    <result property="extension_content" column="extension_content"/>
  </resultMap>  
 
	<sql id="modelTableName">
	    <![CDATA[
	    	t_vas_users_oauth_state
	    ]]>
	</sql>
	  
	<sql id="modelColumns">
	    <![CDATA[
	    	uid,
	    	auid,
	    	identify,
	    	updated_at,
	    	extension_content
	    ]]>
	</sql>  
	
	<sql id="modelInsertValues">
	    	#{uid},
	    	#{auid},
	    	#{identify},
	    	#{updated_at},
	    	#{extension_content}
	</sql> 
	
	<sql id="modelUpdateValues">
			uid=#{uid},
			auid=#{auid},
	    	updated_at=#{updated_at},
	    	extension_content=#{extension_content}
	</sql> 		
	
	<sql id="modelWithoutPKColumns">
	    <![CDATA[
	    	auid,
	    	updated_at,
	    	extension_content
	    ]]>
	</sql>   

	<sql id="model_CRITERIA_SQL_GenCondition">
		<!--
			WARNING - @mbggenerated This element is automatically generated by
			MyBatis Generator, do not modify. This element was generated on Thu
			Sep 01 10:08:21 CST 2011.
		-->
		<where>
			<foreach collection="oredCriteria" item="criteria" separator="or">
				<if test="criteria.valid">
					<trim prefix="(" suffix=")" prefixOverrides="and">
						<foreach collection="criteria.criteria" item="criterion">
							<choose>
								<when test="criterion.noValue">
									and ${criterion.condition}
                				</when>
								<when test="criterion.singleValue">
									and ${criterion.condition} #{criterion.value}
                				</when>
								<when test="criterion.betweenValue">
									and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                				</when>
								<when test="criterion.listValue">
									and ${criterion.condition}
									<foreach collection="criterion.value" item="listItem" open="(" close=")" separator=",">
										#{listItem}
                  					</foreach>
								</when>
							</choose>
						</foreach>
					</trim>
				</if>
			</foreach>
		</where>
	</sql>
	
	<select id="select" resultMap="modelResult">
	    select <include refid="modelColumns" /> from <include refid="modelTableName" /> 
			<where>
		    	<if test="_parameter != null">
					uid = #{uid} and identify = #{identify}
				</if>
			</where>
	</select>
	
	<delete id="deleteByPrimaryKey" parameterType="userOAuthStatePK">
		delete from <include refid="modelTableName" /> where uid=#{uid} and identify=#{identify}
	</delete>
	
	<delete id="delete" parameterType="userOAuthState">
		delete from <include refid="modelTableName" /> where 
			<if test="_parameter != null">
				uid = #{uid} and identify = #{identify}
			</if>
	</delete>
	
	<insert id="insert" parameterType="userOAuthState">
		insert into <include refid="modelTableName" /> (<include refid="modelColumns" />) values (<include refid="modelInsertValues" />);
	</insert>
 
	<update id="update" parameterType="userOAuthState">
	    update <include refid="modelTableName" /> set <include refid="modelUpdateValues" /> where uid = #{uid} and identify = #{identify}
	</update>
		
	<select id="selectByIds" resultMap="modelResult" parameterType="java.util.List">
	  	select <include refid="modelColumns" /> from <include refid="modelTableName" />
	  	<where>
	  		<foreach item="item" index="index" collection="list" open="(" separator="or" close=")">
			    (	uid = #{item.uid} and identify= #{item.identify}	)
			</foreach>
	  	</where>
	</select>
	  
	<delete id="deleteByIds" parameterType="java.util.List">
	  	delete from <include refid="modelTableName" />
	  	<where>
	  		<foreach item="item" index="index" collection="list" open="(" separator="or" close=")">
			    (	uid = #{item.uid} and identify= #{item.identify}	)
			</foreach>
	  	</where>
	</delete>
	
	<select id="pkSelectByModelCriteria" resultType="userOAuthStatePK">
		select uid,identify from <include refid="modelTableName" /> 
		<if test="_parameter != null"> 
	      	<include refid="model_CRITERIA_SQL_GenCondition" />
	      	<if test="orderByClause != null">
				order by ${orderByClause}
	    	</if>
			<if test="start!=0 or limit!=0">
				limit #{limit} OFFSET #{start}
			</if>
	    </if>  
	</select>
	
	<select id="countByModelCriteria" resultType="int">
		select count(*) from <include refid="modelTableName" /> 
		<if test="_parameter != null"> 
	      <include refid="model_CRITERIA_SQL_GenCondition" />
	    </if>  		
	</select>
	
	<delete id="removeByModelCriteria">
		delete from <include refid="modelTableName" />  
		<if test="_parameter != null"> 
	      <include refid="model_CRITERIA_SQL_GenCondition" />
    	</if>  
	</delete>	
</mapper>