<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bhu.vas.api.rpc.statistics.model.FincialStatistics">

  <resultMap id="modelResult" type="fincialStatistics">
    <id property="time" column="time"/>
    <result property="ctm" column="ctm"/>
    <result property="cpm" column="cpm"/>
    <result property="cta" column="cta"/>
    <result property="cpa" column="cpa"/>
    <result property="ctw" column="ctw"/>
    <result property="cpw" column="cpw"/>
  </resultMap>    
  
  	<sql id="modelTableName">
	    <![CDATA[
	    	t_fincial_statistics
	    ]]>
	</sql>
	<sql id="modelColumns">
	    <![CDATA[
	    	time,
	    	ctm,
	    	cpm,
	    	cta,
	    	cpa,
	    	ctw,
	    	cpw
	    ]]>
	</sql>  
	<sql id="modelWithoutPKColumns">
	    <![CDATA[
	    	ctm,
	    	cpm,
	    	cta,
	    	cpa,
	    	ctw,
	    	cpw
	    ]]>
	</sql>   
	
	<sql id="modelInsertValues">
	    	#{time},
	    	#{ctm},
	    	#{cpm},
	    	#{cta},
	    	#{cpa},
	    	#{ctw},
	    	#{cpw}
	</sql> 
	
	<sql id="modelUpdateValues">
	    	created_at = #{created_at},
	    	updated_at = #{updated_at},
	    	extension_content = #{extension_content}
	</sql>
	
	<sql id="model_CRITERIA_SQL_GenCondition">
		<!--
			WARNING - @mbggenerated This element is automatically generated by
			MyBatis Generator, do not modify. This element was generated on Thu
			Sep 01 10:08:21 CST 2011.
		-->
		<where>
			<foreach collection="oredCriteria" item="criteria" separator="or">
				<if test="criteria.valid">
					<trim prefix="(" suffix=")" prefixOverrides="and">
						<foreach collection="criteria.criteria" item="criterion">
							<choose>
								<when test="criterion.noValue">
									and ${criterion.condition}
                				</when>
								<when test="criterion.singleValue">
									and ${criterion.condition} #{criterion.value}
                				</when>
								<when test="criterion.betweenValue">
									and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                				</when>
								<when test="criterion.listValue">
									and ${criterion.condition}
									<foreach collection="criterion.value" item="listItem" open="(" close=")" separator=",">
										#{listItem}
                  					</foreach>
								</when>
							</choose>
						</foreach>
					</trim>
				</if>
			</foreach>
		</where>
	</sql>
	
	<select id="select" resultMap="modelResult">
	    select <include refid="modelColumns" /> from <include refid="modelTableName" /> 
			<where>
		    	<if test="_parameter != null">
					time = #{time}
				</if>
			</where>
	</select>

	<delete id="deleteByPrimaryKey" parameterType="string">
		delete from <include refid="modelTableName" />  where time = #{time}
	</delete>
	
	<delete id="delete" parameterType="userBrandStatistics">
		delete from <include refid="modelTableName" /> 
			<if test="_parameter != null">
					where  time = #{time}
			</if>
	</delete>		
	 
	<insert id="insert" parameterType="userBrandStatistics">
	    insert into <include refid="modelTableName" /> (<include refid="modelColumns" />) values (<include refid="modelInsertValues"/>)
	</insert>
 
  
	<update id="update" parameterType="userBrandStatistics">
	    update <include refid="modelTableName" /> set  <include refid="modelUpdateValues" /> where time = #{time}
	</update>



	<select id="selectByIds" resultMap="modelResult" parameterType="java.util.List">
		select <include refid="modelColumns" /> from <include refid="modelTableName" />
		<where>
			time IN
			<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
				#{item}
			</foreach>
		</where>
	</select>

	<delete id="deleteByIds" parameterType="java.util.List">
		delete from <include refid="modelTableName" />
		<where>
			time IN
			<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
				#{item}
			</foreach>
		</where>
	</delete>
	
	<select id="pkSelectByModelCriteria" resultType="string">
		select time from <include refid="modelTableName" />
		<if test="_parameter != null"> 
	      	<include refid="model_CRITERIA_SQL_GenCondition" />
	      	<if test="orderByClause != null">
				order by ${orderByClause}
	    	</if>
			<if test="start!=0 or limit!=0">
				limit #{limit} OFFSET #{start}
			</if>	
	    </if>  
	</select>
	
	<select id="countByModelCriteria" resultType="int">
		select count(*) from <include refid="modelTableName" /> 
		<if test="_parameter != null"> 
	      <include refid="model_CRITERIA_SQL_GenCondition" />
	    </if>  		
	</select>
	
	<delete id="removeByModelCriteria">
		delete from <include refid="modelTableName" />  
		<if test="_parameter != null"> 
	      <include refid="model_CRITERIA_SQL_GenCondition" />
    	</if>  
	</delete>
</mapper>